# 开发计划

> 这里只维护未完成任务、优先级、依赖和验收条件；已完成工作的变更与验证见 [开发日志](开发日志.md)。项目采用三环混合模式：环 A 完成前期确认，环 B 迭代交付 MVP，环 C 完成架构演进与正式验收。

## 当前进度

| 环 | 状态 |
|:---:|:---|
| A：前期确认（问题定义 + 可行性研究） | 已完成（2026-07-11—07-18） |
| B：MVP 交付 | S1、S2 已归档；S3 执行中 |
| C：架构演进与正式验收 | 待启动 |

## 环 B：MVP 交付

以 1—2 周为一轮冲刺；历史任务和验证证据见开发日志。S3 尚有以下任务：

| 编号 | 任务 | 阶段 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|:---:|---|
| B12 | 根据实际用户反馈调整优先级 | ⑧ | P1 | 自然收集 | 整理至少 10 条有效反馈，形成可执行调整项 |
| B13 | 按需开展轻度后端沙箱实验 | sandbox | P2 | 待需求 | 先明确实验问题和退出条件，不提前引入后端 |
| B14 | 收尾环节自动挡启用：`min-review transcripts / feedback` 两命令（collect-news.yml 已注释占位，标注“见开发计划”）从手动执行为 CI 自动挡；**关键词提纯明确保持非自动**，只能在维护者完成首次审核后显式运行 `bat/after-first-review.bat` | ⑧ | P2 | 待需求 | 先确认字幕请求和反哺待补卡产物有稳定人工处理链路，再在 workflow 启用；不得把 `refine` 接入采集主链或 CI 自动执行 |
| B14-1 | 首次人工审核后关键词提纯真实链路验收：审核结论 apply → DeepSeek refine 与 ai-top 并行产物 → 维护者填写 `adopted_keywords` → refine-apply 幂等写回 | ⑧ | P2 | 待真实数据/成本确认 | 运行前确认将产生 **1 次关键词提纯 DeepSeek 调用 + 1 次 ai-top DeepSeek 调用**；验证 approved 原文边界、English 四字段清单、未知采纳词拒绝、配置幂等追加；不把测试 mock 结果视为真实 API 验收 |
| B14-2 | 热点候选手动归档：维护者确认当天审核、提纯、AI top 和必要发布完成后，运行唯一入口 `bat/archive-min.bat`；归档仅保存 id/title，最近 30 批；归档成功后再重置 `data/manual` 当日人工清单 | ⑧ | P2 | 已解决（2026-08-09） | 不接入自动采集；轻量历史写入成功后才清空候选（[log-entry-59](开发日志.md#log-entry-59)）；清单文件名去日期 + review 追加合并 + 归档重置清单 + 采集/代码时区统一北京时间（[log-entry-60](开发日志.md#log-entry-60)） |

### 热点管线优化后备清单

由 2026-07-31 性能审查产出；已完成的项见 [开发日志.md](开发日志.md#log-entry-30)。以下候选需按优先级推进，改动涉及行为时先确认规则再实施。

| 编号 | 任务 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|---|
| N-P1 | 统一时间层分类：统一 `classifyTimeLayer` 两套实现（build-news 与 news-scheduler 边界语义不一致）；减少 `published_at` 重复 `new Date` 解析 | P1 | 已解决（2026-08-05） | 业务规则已确认（用户拍板）：**单一实现 + 策略参数**；超窗保留 `older`；未来归 `recent-1d`；无效统计归 `older`。news-scheduler 的 classifyTimeLayer 加 `{ future:'recent'\|'none', overflow:'older'\|'none', invalid:'older'\|'none' }` 策略（默认保持调度 null 语义）；build-news 删除独立实现、真正转发 scheduler（统计策略行为不变），修复「注释谎称转发」；排序预解析时间戳（N-P4 印证热点，100k 从 ~400ms 降至 ~30-70ms，约 7-10×）；新增策略参数测试，160/160 通过。详见 [开发日志.md](开发日志.md#log-entry-44) |
| N-P2 | Registry 保留策略：`registry_retention_days=270` 已配置但未生效，Registry 无界增长（2026-08-03 已增至 3475 条 / 3.05MB，`src/` 零使用该配置） | P1 | 已解决（2026-08-05） | 保留语义已定义并拍板：以 `last_seen_at` 起算保留 `registry_retention_days` 天（采集回溯最深 270 天，超期记录不可能再被采集遇到，删除安全）；build 每轮结束自动裁剪 + 归档 `news-registry-pruned.json`（run_id/规则/时间+记录全文，可回滚）+ `stats.last_prune` 审计 + coverage `pruned_in_run`；CLI `registry prune`（默认 dry-run，`--apply` 裁剪）；4 项纯函数单测 + 1 项 build 集成测试，166/166 通过。真实数据 last_seen_at 全部 <30 天，当前裁剪 0 条零即时影响。详见 [开发日志.md](开发日志.md#log-entry-46) |
| N-P3 | 配置契约澄清：`max_pages_per_source_layer`、`max_items_per_source_layer`、`quota_low_watermark`、`concurrency` 等已声明但部分未生效 | P2 | 已解决（2026-08-05） | 30 字段逐项盘点：20+ 已生效、4 个死字段（quota_low_watermark/max_pages/max_items/lock_stale_after_ms）、1 个半生效（concurrency 分类硬编码 5）。用户拍板接线三项预算（低水位早停 + 回溯页/条数硬上限，达限强制终态）+ 移除 lock_stale_after_ms（锁不自动过期为刻意设计）+ concurrency 分类统一配置值。169/169 测试通过。详见 [开发日志.md](开发日志.md#log-entry-47) |
| N-P4 | 建立 1k/10k/100k 确定性 benchmark，验证排序、日期解析、MAD、关键词与 RSS 正则等 CPU 热点 | P2 | 已执行（2026-08-05，基准已记录）；**脚本已随 v1 删除**（2026-08-08） | 曾新增 [scripts/benchmark-news.js](scripts/benchmark-news.js)（四场景合成输入 × 1k/10k/100k）；100k 前三热点为 RSS parseFeed ~938ms、排序 ~400ms、assessItem ~505ms；排序热点主要由 `new Date` 在比较器内重复解析造成，**实测印证 N-P1**；当前 100 条实际规模亚毫秒无性能问题。v1 删除后 benchmark 脚本（依赖 v1 build-news 纯函数）一并移除，性能基线结论保留在 [开发日志.md](开发日志.md#log-entry-43) 与 [开发日志.md](开发日志.md#log-entry-44) |
| N-P5 | 全量 JSON + 同步写扩展性：registry/state 增长后评估分片、NDJSON 或 SQLite | P2 | 待 profile | 保持原子写、fsync、内部状态先写/热点最后写语义；作为独立架构演进，不削弱既有保证 |
| N-P6 | 去重键语义：`dedupeItems` 实际按 `platform:native_id`，注释宣称按 URL+title；与 registry 的 URL fallback 不一致 | P2 | 已解决（2026-08-05） | 真实重复率分析完成（原 [scripts/np6-analysis.js](scripts/np6-analysis.js)，**脚本已随 v1 删除**）：候选层 / registry 三种键（native_id / URL / url+title）均**零重复**，两语义当前观察等价；用户拍板方案 A——保留 `platform:native_id`（与 registry 主键一致），修正 build-news.js:760 过时注释，导出 dedupeItems 并补测试锁定「跨平台同内容不去重、由溯源保留」。v2 主链下 `dedupeItems` 在 projection.js，去重语义不变。161/161 测试通过。详见 [开发日志.md](开发日志.md#log-entry-45) |

### B16 遗留确认项

由 2026-08-03 只读核对与红/橙 bug 修复产出；红色与橙色问题已修复（见 [开发日志.md](开发日志.md#log-entry-35)），以下为仍需确认或推进的次要项。

| 编号 | 任务 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|---|
| B16-R1 | 决策 9.8.2 概念联动匹配字段：当前仅匹配 `term`/`full_name`，未使用 `aliases` 与稳定概念 ID | P2 | 已解决（2026-08-03 确认走适配层方案 B，见 [docs/decisions.md ADR-007](docs/decisions.md)） | 不改写 glossary.json；正式记录适配层方案（`searchConceptKey` 合成稳定 ID + `term`/`full_name` 匹配 + aliases 扩展缝），ADR-007 行号引用已刷新 |
| B16-R2 | 字幕 enrichment 默认关闭（`transcript_enabled: false`）：确认是否为期望状态 | P2 | 已确认（2026-08-04） | 保持默认关闭为期望状态（按需启用：等 L1 AI 渠道接入后置 `true`，配置参数已齐全）；配置不变 |
| B16-R3 | 清理平台筛选相关过时注释（app.js 21/2546/3508、style.css 1693） | P3 | 已解决（2026-08-03） | 原引用行号已因重构漂移；现 app.js:21 已为「内容类型筛选+最近/热度排序」，全仓平台注释均说明「平台属来源核验信息」、与实现一致，无过时残留 |
| B16-R4 | 候选层首次真实采集后复核：公开投影、审核 PR 与 publish 重建流程一致性 | P2 | 已通过（2026-08-04 端到端验证 + 人工审核流启用） | 首次真实采集已填充候选层 100 条并走通审核门禁闭环（pending→publish 剔除→approved→恢复）；人工审核流已启用（新候选默认 pending 待审，公开投影为空时跳过写盘）；验证详情见 [开发日志.md](开发日志.md#log-entry-40) |
| B16-R5 | 路径 A 内容类型填充：`content_type` 当前 100 条全 `unclassified`，热点类型筛选区因此隐藏（决策 65/66/79） | P2 | 已解决（2026-08-05，DeepSeek L1 分类接入） | L1 已接入：新增 llm-provider.js（DeepSeek 封装、fetch 注入、失败自动降级）、classifyCandidate/classifyCandidates 真调用（失败回退 L0）、CLI 透传 --model、build-news 候选创建阶段接入分类器（L0 恒兜底、L1 显式启用）、mergeCandidates 保留人工 reviewed 结论；真实 key 联调两条分类正确（ai_product / ai_industry），159/159 测试通过。生产启用需配置 DEEPSEEK_API_KEY（未配置自动退化 L0 不影响构建）；每轮 ≤100 次分类调用成本约几分钱。详情见 [开发日志.md](开发日志.md#log-entry-42) |
| B16-R6 | tools.json 补充工具唯一发布时间字段（ADR-009 后续）：45 个工具无 `published_at`/`release_date` 等字段，工具/场景卡全显示「发布时间待补充」 | P2 | 已解决（2026-08-04） | 28 个工具已全部补齐 `published_at`（取各工具当前最新版本模型正式发布时间）；17 个产品型/日期未确认工具已按用户决定从工具库移除，CLAUDE.md 声明已同步；工具卡/场景卡自动显示实际发布时间 |
| B16-R7 | 热点 `related_resources` 稳定 ID 填充（ADR-008 后续）：当前 0/100，关联资料区固定显示「暂不可用」 | P2 | 已解决（2026-08-05） | **更正失真记录**：原「已为 69/100 填充」与 git 全量历史不符（各提交公开投影/候选层均 0/100，无词边界脚本痕迹），已取证定位。用户拍板**方案 A**：URL 精确身份匹配 + **标题词边界匹配**双维度（buildRelatedTitleLexicon 工具35/概念85/场景12 + titleContainsKeyword 中文子串/英文词边界 + matchRelatedByTitle 去重/优先级/≤3上限 + searchConceptKey ADR-007 稳定ID），实测填充 **19/100**（22 条关联）、幂等、前端零改动联动已验证。39/39 news、184/184 全量、validate 原则 1-6 通过。详见 [开发日志.md](开发日志.md#log-entry-49) |
| B16-R8 | b16-content-type-fix-plan.md 状态同步：「规划中，只做方案不改代码」→「路径 B 已落地」 | P3 | 已解决（2026-08-03） | 文档头部 / §4 已选路径 / §9 实施状态 / §11 达成度已标注路径 B 落地与路径 A 待办，与实现一致 |
| B16-R9 | 密钥治理：DeepSeek key 只经环境变量注入（B16-R5 生产启用前置） | P1 | 已解决（2026-08-05） | 新增零依赖 loadDotEnv（.env 已 gitignore，入口自动加载，不覆盖 CI Secrets）+ check-secrets 扫描守卫（8 模式自检通过 + 全仓 220 文件零命中）+ validate.js 原则6 + CI guard 步骤 + build env 预留 DEEPSEEK_API_KEY 注入 + .env.example 模板；178/178 测试通过；真实 key 经 git 全量取证（工作树/可达提交/reflog/不可达 blob）确认从未入库。生产启用：本地填 .env、CI 加 GitHub Secret。详见 [开发日志.md](开发日志.md#log-entry-48) |
| B16-R10 | 前端 ES module 拆分后补自动化测试：data/search/tools/compare/trending/scenes/featured/glossary/main 现零测试，依赖人工核验（2026-08-06 代码库解耦重构后） | P3 | 待确认 | 先定测试栈（node:test + jsdom，或轻量浏览器跑测）；目标锁定 import/export 契约、8 视图渲染关键路径与跨模块跳转，不引入打包器 |
| B16-R11 | 热点详细采集统一总开关：GitHub Repository Variable `NEWS_COLLECTION_ENABLED` 与版本化 `collection.enabled` 双层门禁 | P1 | 已解决（2026-08-11） | `NEWS_COLLECTION_ENABLED=false` 时 GitHub Actions 的含 Key 采集 job 按条件跳过；项目开关严格要求布尔 `true`，关闭时管线零网络、零写入；本地 153/153 测试、validate 和 diff 检查通过。远程 Actions 未由本机重复触发，避免消耗平台配额。详见 [开发日志.md](开发日志.md#log-entry-63) |
| B16-R12 | X 采集后台 credits 超预算修复：请求级预占、重试计数与运行用量审计 | P1 | 已解决（2026-08-12，边界复核已补齐） | X tweet/article 每次请求尝试受 3750 硬上限保护；显式零预算与非法预算 fail closed，供应商单价/单页安全下界不可被配置调低，超量响应按完整条数结算并止损；credits/request 数据透传至 `last-run.json`、CLI 与 Actions Summary，并由 validate 校验配置和账本；单平台失败/partial 的总状态已修正。166/166 全量测试、validate、语法检查和 diff 检查通过。详见 [开发日志.md](开发日志.md#log-entry-64) |
| B16-R13 | 卡片生成器联网搜索修复与 provider/model 可切换：入口补 `loadDotEnv` + DeepSeek web_search 两段式工具循环 + 统一 Responses provider 配置 | P1 | 已解决（2026-08-14） | 根因：DeepSeek Responses API 的 `web_search` 为两段式（第一段只返回 `web_search_call`，须全量回传含 `reasoning_text` 才恢复搜索结果），原代码单段请求从不回传故 0 来源。现已补 `loadDotEnv()`；新增共享搜索模块和 provider registry；catalog 按大模块读取 `provider/model/protocol`，DeepSeek 自动两段式，OpenAI 等 Responses provider 自动单段，Messages API 仅预留且 fail-closed；Key 由 provider 映射到 `.env` 环境变量。181/181 测试、validate、密钥扫描和 diff 检查通过；未执行真实付费 probe/new。详见 [开发日志.md](开发日志.md#log-entry-68) |
| B10-UI | 工具库厂商/工具双视图与侧边索引用户试用收尾 | P2 | 试用中（2026-08-11） | 已完成目录解耦、工具投影、四类分组、侧边索引定位和极简科技风格；待用户刷新 `dist/index.html` 后确认桌面/移动端视觉、字号和间距，后续只做局部样式微调 |

### 工具目录五模块解耦收口

五模块数据、Node/浏览器 catalog Interface 和工具目录主路径已落地；以下只保留尚未完成的严格收口与真实验收任务，已完成证据见 [开发日志.md](开发日志.md#log-entry-65)。

| 编号 | 任务 | 优先级 | 状态 | 依赖/验收条件 |
|---|---|:---:|:---:|---|
| B10-D1 | 清理前端兼容投影：将精选、对比、场景、搜索和热点消费者从 `data.js` 的 `tools`/`toolIntelligence` 只读投影迁移到 catalog Interface 与稳定引用 | P1 | 待推进 | 先分别确认各视图需要的公开 DTO；迁移完成后删除兼容状态、`buildLegacy*`、`buildDirectoryProjection` 及相关旧查询函数；不得恢复直接读取旧 JSON |
| B10-D2 | 清理 `tools.js` 旧树形渲染残留 | P2 | 待推进 | 仅保留页面协调、模态和稳定引用导航；删除不再被主路径调用的旧树形渲染/适配函数；五个展示模块之间不得相互 import |
| B10-D3 | 完善稳定引用消费者与目录契约测试 | P1 | 待推进 | 覆盖厂商卡片→一级→二级→三级、工具卡片→三级的同一详情身份；覆盖精选、对比、场景、搜索、热点的目标解析、未知引用和渲染关键路径；不引入打包器 |
| B10-D4 | 更新旧文件名引用的工程文档和贡献说明 | P2 | 待推进 | 清理仍把 `tools.json`/`tool-intelligence.json` 描述为当前事实来源的 docs、CONTRIBUTING 和非历史说明；保留历史日志中的真实变更记录，不篡改历史事实 |
| B10-D5 | 执行真实浏览器与移动端回归 | P1 | 待浏览器驱动 | 具备可用 Chromium/Chrome 自动化驱动后，验证厂商/工具双视图、搜索筛选、两条路径进入同一三级详情、对比、场景、精选、热点、模态焦点和移动端布局；需包含截图或可复核交互证据 |

### B10-D6 字段契约收缩

前两批字段清理已完成，结果见 [开发日志.md](开发日志.md#log-entry-66)。本项只追踪已完成的字段所有权收紧，不代表整体兼容投影已经删除。

| 编号 | 任务 | 优先级 | 状态 | 验收/结果 |
|---|---|:---:|:---:|---|
| B10-D6 | 清理五模块无用/重复字段并保持兼容路径 | P1 | 已解决（2026-08-13） | 删除厂商卡片旧详情/派生字段、工具卡片四个重复字段、二级 `child_previews`、一级 `tree_mode`、三级 `parent_level2_ref`/`relation_source_refs`/`compare`；更新迁移脚本、兼容投影、字段白名单和文档；173/173 测试、validate、build-dist、引用检查通过。 |
| B10-D7 | 目录生成器真实联网链路稳定性与成本门禁收口 | P1 | 待修复（2026-08-14 真实联调发现） | 搜索达到轮次上限、空文本、`incomplete/failed` 和非 Evidence JSON 必须分别 fail-closed 并保存有限诊断；真正执行 `max_search_queries`/`max_pages`/`max_ai_calls`；为 catalog 草案补完整 JSON Schema 和嵌套字段类型校验；让 `discovery_sources` 参与检索；新增上述边界的 mock 回归后，再进行受成本确认保护的真实 `new` 验收。详见 [开发日志.md](开发日志.md#log-entry-69) |

### 前端 i18n / 多语言扩展（试点已落地）

由 2026-08-07「前端 i18n 框架 + 热点中文化」产出（[开发日志.md](开发日志.md#log-entry-53)）。i18n 框架已建立（两层：UI 文案 `t()` 读 `i18n/zh.js` 字典 + 内容 `getLocalizedField` 读 `localizations[locale]`），试点接入热点视图 + data.js 共享工具；以下为整个前端汉化/多语言的后续扩展项。

| 编号 | 任务 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|---|
| L10N-1 | 整个前端 UI 文案接入 i18n：其余 6 个视图（tools/search/compare/featured/glossary/scenes）+ index.html 其余部分 + main.js 其余文案 → `t()` / `data-i18n` | P2 | 待推进 | 按视图逐批接入；`t()` 缺 key 回退 zh → 原 key，未接入文案不破坏 UI |
| L10N-2 | 内容本地化扩展到其他视图：tools.json / glossary / scenes / featured 数据加 `localizations` 字段（数据侧 content-localizer 已就绪，`localize_target_locale` 通用） | P2 | 待推进 | 各数据文件支持 `localizations` + 前端 `getLocalizedField` 接入；当前 localize 只作用于 news 候选层 |
| L10N-3 | 更多语言支持：新增 `en.js` 字典 + `setLang` 接线 + 语言切换 UI（当前 `SUPPORTED_LANGS=['zh']`） | P3 | 待推进 | 中文稳定后推第二语言；切换需 `applyStaticTranslations` + 各视图重渲染 |

> 环 B 结束条件：MVP 功能稳定、反馈机制就位、公开 URL 可访问。

## 环 C：架构演进与正式验收

环 B 经用户反馈验证后启动。先正式化总体设计，再按依赖推进详细设计、实现和验收。

| 编号 | 任务 | 阶段 | 依赖/时序 |
|:---:|---|:---:|---|
| C01 | 正式化 v1.0 目标架构（SPA + Serverless）、技术栈、模块和演进路线 | ④ | 环 B 稳定后 |
| C02 | 基于 MVP 反馈复核需求基线，补全 P0 条目和验收条件 | ③ | 与 C01 同期 |
| C03 | 按需定义关键模块的数据字段、接口和组件 | ⑤ | C01 后 |
| C04 | 实现 Serverless API 代理、定时采集和推荐引擎等方案 3 能力 | ⑥ | C03 后 |
| C05 | 实现插件注册、分发和加载机制 | ⑥ | C04 后端基础完成后 |
| C06 | 实现 OAuth、偏好设置、定时调度和多平台订阅推送 | ⑥ | C04 后端基础和 C05 后 |
| C07 | 基于 localStorage 推断偏好，支持冷启动与破茧机制 | ⑥ | 可与 C06 并行 |
| C08 | 按使用频率估算工具月/年费用 | ⑥ | P1 |
| C09 | 完成集成测试和验收测试报告 | ⑦ | 编码完成后 |
| C10 | 完善 PR 指南、贡献流程和社区推广 | ⑧ | 与 C09 并行 |
| C11 | 访谈职场人士、开发者和创作者，补充非学生样本 | ③ | 编码期间穿插 |
| C12 | 发布 v1.0（功能、文档和社区就位） | ⑧ | 全部完成后 |

## 路线依据

可行性结论为 **Conditional Go**：先以静态站验证价值，再演进至 SPA + Serverless。实现时保持模块边界清晰、路径集中、同模块编码模式一致，并先确定数据结构和接口再扩展逻辑。
