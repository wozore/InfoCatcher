# 开发计划

> 这里只维护未完成任务、优先级、依赖和验收条件；已完成工作的变更与验证见 [开发日志](开发日志.md)。项目采用三环混合模式：环 A 完成前期确认，环 B 迭代交付 MVP，环 C 完成架构演进与正式验收。

## 当前进度

| 环 | 状态 |
|:---:|:---|
| A：前期确认（问题定义 + 可行性研究） | 已完成（2026-07-11—07-18） |
| B：MVP 交付 | S1、S2 已归档；S3 执行中 |
| C：架构演进与正式验收 | 待启动 |

## 环 B：MVP 交付

以 1—2 周为一轮冲刺；历史任务和验证证据见开发日志。S3 尚有以下任务：

| 编号 | 任务 | 阶段 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|:---:|---|
| B12 | 根据实际用户反馈调整优先级 | ⑧ | P1 | 自然收集 | 整理至少 10 条有效反馈，形成可执行调整项 |
| B13 | 按需开展轻度后端沙箱实验 | sandbox | P2 | 待需求 | 先明确实验问题和退出条件，不提前引入后端 |

### 热点管线优化后备清单

由 2026-07-31 性能审查产出；已完成的项见 [开发日志.md](开发日志.md#log-entry-30)。以下候选需按优先级推进，改动涉及行为时先确认规则再实施。

| 编号 | 任务 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|---|
| N-P1 | 统一时间层分类：统一 `classifyTimeLayer` 两套实现（build-news 与 news-scheduler 边界语义不一致）；减少 `published_at` 重复 `new Date` 解析 | P1 | 待确认规则 | 先确认未来日期与超 270 天内容的业务规则（拒绝/归最近层/标记异常；`older` 保留或无层），再收敛为单一实现并加参数化测试。已确认差异（2026-08-03）：build-news.js:762 用 `Math.max(0,·)`、未来时间归 recent-1d、超窗返回 `'older'`，且注释谎称「转发到 news-scheduler 的实现」实为独立实现；news-scheduler.js:71 未来/超窗返回 `null` |
| N-P2 | Registry 保留策略：`registry_retention_days=270` 已配置但未生效，Registry 无界增长（2026-08-03 已增至 3475 条 / 3.05MB，`src/` 零使用该配置） | P1 | 待设计 | 定义保留语义（去重/审计/历史回溯/授权所需），设计可审计清理与迁移/回滚，再实施 |
| N-P3 | 配置契约澄清：`max_pages_per_source_layer`、`max_items_per_source_layer`、`quota_low_watermark`、`concurrency` 等已声明但部分未生效 | P2 | 待确认契约 | 明确字段业务语义（单次/累计/恢复期上限），在 collector/scheduler 边界强制预算并补测试 |
| N-P4 | 建立 1k/10k/100k 确定性 benchmark，验证排序、日期解析、MAD、关键词与 RSS 正则等 CPU 热点 | P2 | 待执行 | 生成合成输入（高离散主题/单一大来源/大量重复/边界时间），记录耗时与 heap，确认后再优化 |
| N-P5 | 全量 JSON + 同步写扩展性：registry/state 增长后评估分片、NDJSON 或 SQLite | P2 | 待 profile | 保持原子写、fsync、内部状态先写/热点最后写语义；作为独立架构演进，不削弱既有保证 |
| N-P6 | 去重键语义：`dedupeItems` 实际按 `platform:native_id`，注释宣称按 URL+title；与 registry 的 URL fallback 不一致 | P2 | 待确认 | 统计真实重复率并与业务确认后再决定是否修正，防止输出条数/评分变化。已确认（2026-08-03）：build-news.js:758 注释「按 url+title 去重」与 :854 实现 `platform:native_id` 不符 |

### B16 遗留确认项

由 2026-08-03 只读核对与红/橙 bug 修复产出；红色与橙色问题已修复（见 [开发日志.md](开发日志.md#log-entry-35)），以下为仍需确认或推进的次要项。

| 编号 | 任务 | 优先级 | 状态 | 验收/启动条件 |
|:---:|---|:---:|:---:|---|
| B16-R1 | 决策 9.8.2 概念联动匹配字段：当前仅匹配 `term`/`full_name`，未使用 `aliases` 与稳定概念 ID | P2 | 已解决（2026-08-03 确认走适配层方案 B，见 [docs/decisions.md ADR-007](docs/decisions.md)） | 不改写 glossary.json；正式记录适配层方案（`searchConceptKey` 合成稳定 ID + `term`/`full_name` 匹配 + aliases 扩展缝），ADR-007 行号引用已刷新 |
| B16-R2 | 字幕 enrichment 默认关闭（`transcript_enabled: false`）：确认是否为期望状态 | P2 | 已确认（2026-08-04） | 保持默认关闭为期望状态（按需启用：等 L1 AI 渠道接入后置 `true`，配置参数已齐全）；配置不变 |
| B16-R3 | 清理平台筛选相关过时注释（app.js 21/2546/3508、style.css 1693） | P3 | 已解决（2026-08-03） | 原引用行号已因重构漂移；现 app.js:21 已为「内容类型筛选+最近/热度排序」，全仓平台注释均说明「平台属来源核验信息」、与实现一致，无过时残留 |
| B16-R4 | 候选层首次真实采集后复核：公开投影、审核 PR 与 publish 重建流程一致性 | P2 | 已通过（2026-08-04 端到端验证 + 人工审核流启用） | 首次真实采集已填充候选层 100 条并走通审核门禁闭环（pending→publish 剔除→approved→恢复）；人工审核流已启用（新候选默认 pending 待审，公开投影为空时跳过写盘）；验证详情见 [开发日志.md](开发日志.md#log-entry-40) |
| B16-R5 | 路径 A 内容类型填充：`content_type` 当前 100 条全 `unclassified`，热点类型筛选区因此隐藏（决策 65/66/79） | P2 | 待确认渠道 | 先确认 LLM 渠道与额度（每轮 ≤100 次分类调用，见 b16-content-type-fix-plan §8），再实现分类模块 + CLI 级类型确认；前端无需改动 |
| B16-R6 | tools.json 补充工具唯一发布时间字段（ADR-009 后续）：45 个工具无 `published_at`/`release_date` 等字段，工具/场景卡全显示「发布时间待补充」 | P2 | 待执行 | 补字段后工具卡与场景卡自动显示实际发布时间 |
| B16-R7 | 热点 `related_resources` 稳定 ID 填充（ADR-008 后续）：当前 0/100，关联资料区固定显示「暂不可用」 | P2 | 待执行 | 构建脚本为审核通过候选补充工具/概念/场景稳定 ID，前端无需改动 |
| B16-R8 | b16-content-type-fix-plan.md 状态同步：「规划中，只做方案不改代码」→「路径 B 已落地」 | P3 | 已解决（2026-08-03） | 文档头部 / §4 已选路径 / §9 实施状态 / §11 达成度已标注路径 B 落地与路径 A 待办，与实现一致 |

> 环 B 结束条件：MVP 功能稳定、反馈机制就位、公开 URL 可访问。

## 环 C：架构演进与正式验收

环 B 经用户反馈验证后启动。先正式化总体设计，再按依赖推进详细设计、实现和验收。

| 编号 | 任务 | 阶段 | 依赖/时序 |
|:---:|---|:---:|---|
| C01 | 正式化 v1.0 目标架构（SPA + Serverless）、技术栈、模块和演进路线 | ④ | 环 B 稳定后 |
| C02 | 基于 MVP 反馈复核需求基线，补全 P0 条目和验收条件 | ③ | 与 C01 同期 |
| C03 | 按需定义关键模块的数据字段、接口和组件 | ⑤ | C01 后 |
| C04 | 实现 Serverless API 代理、定时采集和推荐引擎等方案 3 能力 | ⑥ | C03 后 |
| C05 | 实现插件注册、分发和加载机制 | ⑥ | C04 后端基础完成后 |
| C06 | 实现 OAuth、偏好设置、定时调度和多平台订阅推送 | ⑥ | C04 后端基础和 C05 后 |
| C07 | 基于 localStorage 推断偏好，支持冷启动与破茧机制 | ⑥ | 可与 C06 并行 |
| C08 | 按使用频率估算工具月/年费用 | ⑥ | P1 |
| C09 | 完成集成测试和验收测试报告 | ⑦ | 编码完成后 |
| C10 | 完善 PR 指南、贡献流程和社区推广 | ⑧ | 与 C09 并行 |
| C11 | 访谈职场人士、开发者和创作者，补充非学生样本 | ③ | 编码期间穿插 |
| C12 | 发布 v1.0（功能、文档和社区就位） | ⑧ | 全部完成后 |

## 路线依据

可行性结论为 **Conditional Go**：先以静态站验证价值，再演进至 SPA + Serverless。实现时保持模块边界清晰、路径集中、同模块编码模式一致，并先确定数据结构和接口再扩展逻辑。
