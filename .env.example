# ===== InfoCatcher 本地环境变量模板 =====
# 用法：复制本文件为 .env 并填入真实值。
# .env 已被 .gitignore 忽略，不会进版本库；本模板只含占位符，可安全提交。
# 项目约定：所有密钥只经环境变量注入（进程 env 或 .env），绝不写入代码/配置。
#
# ── DeepSeek L1 内容分类（可选）───────────────────────────────
# 配置后 build-news 自动启用 L1 语义分类；缺省/失败自动回退 L0 规则式，
# 构建永不因 LLM 阻塞（B16 成本/可靠性约定）。
DEEPSEEK_API_KEY=

# ── Tavily 检索（目录生成器官方来源发现/正文抓取）──────────────
# 目录生成器 research 阶段用 Tavily Search/Extract 发现官方来源并抓取正文；
# 缺省时目录生成器 probe/研究失败（fail-closed）。
TAVILY_API_KEY=

# 可选：显式指定 provider 与模型（不设 provider 时，存在 DEEPSEEK_API_KEY 即自动 deepseek）
# INFOCATCHER_CLASSIFY_PROVIDER=deepseek
# INFOCATCHER_CLASSIFY_MODEL=deepseek-chat

# ── 平台采集 key（可选；CI 中也可用 GitHub Secrets 注入同名变量）──
# YOUTUBE_API_KEY=
# X_API_KEY=
