项目开发入口契约审计报告（知览/KnowView）
结论摘要
README 对外契约（4 条命令 + 6 个链接）全部真实有效，零漂移。但内部契约 CODEBASE-MAP.md 严重滞后：漏收 1 个活跃采集入口、2 个孤立脚本、4 个 src 模块、约 25 个测试文件，并有 2 对重复条目与 1 处段归属错误。品牌已基本完成迁移，但代码内环境变量命名与根级杂项文件仍残留旧态。

【文档漂移】文档声称 vs 磁盘实际
README.md 命令/链接全部有效 — README 声明的 5 条命令（python -m http.server 8000、node scripts/build-dist.js、node scripts/validate.js、node --test --test-concurrency=1 tests/、node scripts/browser-acceptance.js）与 6 个链接（CONTRIBUTING/SUPPORT/SECURITY/CODE_OF_CONDUCT/LICENSE/docs/manual/editorial-and-data-policy.md）逐一验证均存在。无"声称存在但磁盘没有"项。建议：无。

scripts/build-news.js 是活跃采集入口但 CODEBASE-MAP 完全未收录 — 证据：.github/workflows/collect-news.yml:116 运行 node scripts/build-news.js --platforms，bat/after-first-review.bat:29 也提示 node scripts\build-news.js；但 grep build-news CODEBASE-MAP.md 零命中，map 却把 scripts/news-cli.js 写为"CLI 分发入口"。建议：map 补录 build-news.js，并说明它才是采集管线实际入口。

scripts/refine-zh-localizations.js 磁盘存在、map 未收录、无任何管线引用 — 自述"一次性回填"脚本（文件头部注释），仅 开发日志.md 提及。建议：确认完成后删除（或移入 docs/archive 归档说明）。

bat/archive-min.bat 磁盘存在但未列入 map scripts 清单 — map 的 bat 清单列了 8 个（identity-review/after-first-review/apply-keywords/apply-top/catalog-generator/concept-generator/tool-update-review/build-dist），磁盘有 9 个，缺 archive-min.bat；仅在 cmd-min.js 条目文字里以"维护者入口"带过。建议：补录。

9 个 bat 引用的脚本全部解析成功 — 逐文件核对（如 apply-top.bat→news-cli.js+publish-news.js、archive-min.bat→news-cli.js、build-dist.bat→build-dist.js 等），无坏引用。建议：无。

【CODEBASE-MAP.md 准确性抽检】
catalog-interface.js 段归属错误 — map 第 58 行把它列在 ## src/catalog/ 段（57-95 行）下，但磁盘文件在 src/catalog-interface.js（src 根）。建议：挪到正确段或更正路径注释。

tool-update-review-contract.js 重复条目 — map 第 77 行与 80 行内容完全相同，同一文件 src/catalog/tool-update-review-contract.js 收录两次（疑似改名时旧条目未删）。建议：删一条。

tool-update-evidence.js 重复条目 — map 第 78 行与 81 行完全相同。建议：删一条。

src/ 磁盘有 4 个文件 map 未收录 —

src/shared/beijing-time.js（北京时间工具，news 管线多处使用）
src/catalog/catalog-snapshot-store.js
src/catalog/ai/catalog-adapters.js（讽刺的是其测试 tests/catalog/catalog-adapters.test.js 在 map tests 段有收录）
src/catalog/ai/concept-synthesis-prompt.js（姐妹文件 concept-synthesis-ai.js 在 map） 建议：4 个全部补录。
src/web 段 19 个 js + css + i18n + icons 全部存在 — map 该段准确，无遗漏。src/comparison（17）、src/news（24）、src/acquisition（4）、src/maintenance（5）、src/content（2）也全部存在。建议：无。

tests 段严重滞后：约 25 个测试文件未收录 — map tests 段收录 40 个，磁盘共 73 个。遗漏包括：comparison 4 个（empty-model-filter、identity-review、model-identity、revision-date）、maintenance 3 个（check-secrets、env、validate-comparison）、shared 1 个（local-model）、news 16 个（beijing-time、content-classifier-llm/rule、feedback/llm-entity-extract、feedback/tool-feedback、keyword-refine、min-history、news-cmd-min、news-localizer、news-public-gate、news-review-list、news-reviewer、news-rss、news-summarizer、review-v2-disclosure、run-after-first-review）、tests/fixtures/x.json。建议：按测试子目录批量补录。

【入口契约现状】
无 package.json / 无 npm — 顶层无 package.json，标准入口即 node scripts/*.js 直跑 + bat 启动器 + GitHub Actions。README 4 命令是唯一对外文档化入口。

从零跑起路径清晰可行 — 校验 node scripts/validate.js（薄包装，require('../src/maintenance/validate')，CI 3 个工作流依赖）→ 测试 node --test --test-concurrency=1 tests/（README 特意限定 tests/ 目录以规避根级 .obsidian 符号链接的递归扫描）→ 构建 node scripts/build-dist.js（dist/ 可丢弃重建）→ 起站 python -m http.server 8000 → 浏览器验收需先 cp config/browser.example.json config/browser.local.json（local 已 gitignore）。建议：无，契约自洽。

维护入口分散且仅 map/docs 记录、README 不覆盖 — 采集 build-news.js、审阅 news-cli.js min-review 命令组、发布 publish-news.js、对比 fetch-comparison.js、目录 catalog-generator.js/concept-generator.js/tool-update-review.js。对普通开发者，这些"入口表面"靠 CODEBASE-MAP 与 docs/manual 发现。建议：可选——README 增一节维护命令索引，降低查找成本。

.github/workflows 6 个全部指向真实脚本 — collect-news.yml（build-news.js + news-cli.js min-review）、deploy.yml（validate.js + build-dist.js）、publish-news.yml（publish-news.js + validate.js）、refresh-comparison.yml、refresh-vibe-hub-cache.yml、weekly-tool-update-review.yml。建议：无。

config/ 契约齐全 — browser.local.json、browser.example.json、catalog-generator.example.json 均在（config/catalog-generator.local.json 按约定缺失属正常）。建议：无。

【结构性混乱 / 遗留物】
品牌已迁移，公开交付面无 InfoCatcher — README/ABOUT/CONTRIBUTING/SUPPORT/SECURITY/CODE_OF_CONDUCT/CONTEXT/public/src/web/.github/ISSUE_TEMPLATE（config.yml 引用 wozore/KnowView）全部干净。残留仅在：

代码内环境变量仍 INFOCATCHER_* 前缀（6 处：content-classifier.js:253、content-localizer.js:80、content-reviewer.js:94、content-summarizer.js:88、local-model.js:22/25，及 .env.example:22-23）— 功能正常、与代码一致，但命名滞后品牌。建议：非紧急；若统一需全局重命名并同步 .env/workflows/文档。
.claude/CLAUDE.md 仍指向 wozore/InfoCatcher-Engineering issue 仓库（该仓库本就是工程 issue 仓，属有意保留还是漏改需确认）。
根级已跟踪杂项与"工程文档不入库"政策矛盾 — 开发日志.md（238K）、开发计划.md、用户反馈收集表.png、skills-lock.json 均被 git 跟踪提交，而 CLAUDE.md/.gitignore 明示"仅 docs/manual/**/*.md 入库"。建议：移入 docs/ 或取消跟踪（迁移前确认两份中文日志为活跃文件）。

.gitignore 声明与已跟踪冲突 — .gitignore 第 34 行忽略 .claude/skills/，但 git ls-files 显示 40+ 个 .claude/skills/**（如 code-review/SKILL.md、ask-matt/openai.yaml）已入库。建议：要么删 .claude/skills/ 忽略规则，要么把第三方 skills 移出（gitignore 不作用于已跟踪文件，属历史遗留）。

孤立/遗留脚本 2 个 — scripts/migrate-catalog-five-modules.js（旧目录迁移，仅 CODEBASE-MAP 提及，无 README/bat/workflow 引用，历史迁移已完成）与 scripts/refine-zh-localizations.js（一次性回填）。建议：确认后删除或移入 docs/archive。

.smoke-cmp2/3/4 三个空目录残留（2026-08-19/20 创建，内容为空）— 建议删除。.claudian/sessions/ 已正确忽略，正常。

dist/ 存在属预期 — gitignore 第 12 行 dist/，磁盘 dist/ 是最近构建产物（符合"可丢弃构建输出"约定）。无问题。

docs/ 工程文档均在磁盘未入库（b16-*、archive/、prototypes/、architecture.md 等）— 与 docs 边界政策一致，仅本地堆积，git 层干净。如不需要可本地清理。

优先级建议：先修 map 重复条目（7/8）与段归属（6），再补录活跃入口 build-news.js（2）与 4 个 src 文件（9）——这几项直接影响 Agent/开发者定位代码；测试段补录（11）量大但机械，可批量处理。删除/归档遗留脚本（20）与空目录（21）风险低，可顺手清理。品牌 env 命名（17）与根级杂项（18）涉及行为变更，建议单独立项确认。

相关文件：e:\Work\AI信息获取软件开发\CODEBASE-MAP.md、e:\Work\AI信息获取软件开发\scripts\build-news.js、e:\Work\AI信息获取软件开发\.github\workflows\collect-news.yml。

仓库卫生审计报告 — wozore/KnowView
总量:454 个跟踪文件，.git 25MB，工作树仅 1 处未提交改动。.env 真实密钥文件未入库（已核验，仅 .env.example 被白名单跟踪），.obsidian/.claudian/dist//worktrees 均未跟踪，无大小写重复文件，无 .DS_Store/*.bak/*.log 入库。整体较干净，问题集中在历史遗留文件与运行时状态入库。

【高】建议处理（删除/修复）
开发日志.md（238KB，根目录，中文名）— 旧生命周期阶段的开发日志，项目约定"开发日志不入库"，无项目代码引用，仅历史提交带入 — 删除或归档到 resources/archive
开发计划.md（20KB，根目录，中文名）— 同上，旧阶段开发计划残留，不被引用 — 删除或归档
用户反馈收集表.png（558KB，根目录，中文名）— 用户反馈表单截图误入根目录，无引用，旧提交"本地ai迁移"带入 — 删除或移到 resources/survey
data/news/runtime/min-candidates.json（5.7MB，唯一 >1MB 跟踪文件）— 热点管线 v2 候选层运行时状态，高频变更（多条 "chore: update min candidates" 提交），仓库唯一超大文件 — 评估移出 git（gitignore + 依赖 CI/历史），或至少做历史瘦身
【中】建议整理
.claude/skills/（68 个文件）— 已跟踪，但 .gitignore:68 明确声明该目录不入库（第三方 skill），规则生效前入库未被清理 — git rm -r --cached .claude/skills/ 后按忽略规则管理
worktree-agent-* 本地分支（23 个）— Claude Code 自动生成的 worktree 分支；其中 6 个无关联 worktree 已废弃：a27ec5f15c71331d4、a429f8d74310459f4、aae4778b1ca9167be、ad2e33908df0963b9、ae4492f3765d94bea、afd22e88b54af19ef — 删除废弃分支，其余随 worktree 清理
origin/news/review/batch-*（10 个远程分支）— 2026-08-09~11 的审核批次分支仍挂在远端，本地无对应 — 远端清理 git push origin --delete
.smoke-cmp2/ .smoke-cmp3/ .smoke-cmp4/（空目录）— browser-acceptance 冒烟残留，git 不可见 — 磁盘上 rm -d 删除
.obsidian（符号链接，磁盘存在）— 指向 /c/Users/HelloWare/OneDrive/Desktop/MyNote/AI归档知识库/.obsidian，目标已缺失（TARGET BROKEN）；git 侧安全（未跟踪且被忽略）— 删除磁盘上损坏链接或恢复目标
data/manual/tools/ai-tools-wave-2026-08-*.json（30+ 文件，含 -repair/-repair-v2/-repair-v3 迭代变体）— 单次批量 wave 的种子与多轮修复文件全部入库，含 manifest/preview — 收敛至 archive 或清理历史迭代变体
【低】观察项
data/manual/archive/vibe-hub-cache.json — 3 天 TTL 的网页抓取缓存被当基线提交，定时刷新会产生变更 — 评估 gitignore，依赖 CI 定时刷新
data/news/config/news-config-v2.说明.md、resources/source-lists/热点信息源清单.md — 中文文件名，Windows 无碍但 Linux CI/工具链有编码风险，且是"应入库"的有意文件 — 保留但可考虑改 ASCII 名
.gitignore 死规则 — 06.编码与单元测试/mvp/... 指向已不存在的旧目录结构，logs/、/output/、/outputs/ 目录均不存在 — 清理失效规则
空目录（磁盘残留，git 不跟踪）：data/catalog/.backup/ .staging/ .transactions/、data/manual/tools/catalog-drafts/、dist/data/catalog/.* — 事务/构建残留 — 随手清理
.claudian/ — 磁盘有 sessions/*.meta.json，未跟踪且 sessions 已忽略；建议整目录忽略以防未来新增文件漏配 — .claudian/ 替代 .claudian/sessions/
E:/Work/knowview-ci-check（仓库外 worktree，detached HEAD @71d1bd7）— CI 检查用独立工作区，非本仓库管理范围 — 确认不用后可移除
其他状态
未提交改动仅 1 处：data/news/output/hotspots.json（M）— 常规运行产物变更，待提交
分支结构：仅 main 主线，无功能分支；data/comparison/raw/、public/、.github/workflows/ 等均为合理入库项，og-image.png 仅 24KB 无异常
核验通过项：.env 无泄漏、dist/ 零跟踪、无 .bak/.DS_Store/日志入库、无大小写重复、无嵌套 .git/子模块
关键文件路径：e:\Work\AI信息获取软件开发\.gitignore、根目录三个中文残留文件、e:\Work\AI信息获取软件开发\data\news\runtime\、e:\Work\AI信息获取软件开发\.claude\skills\。

入口契约那路已经探完，收获不小（CODEBASE-MAP 有重复条目/段归属错误、漏了 build-news.js 等活跃入口）。等另外两路（仓库文件管理、代码优化）也回来再一起汇总给你。

审计完成。以下为只读探测结论（未修改任何文件），按主题分组。

【调试残留】— 基本干净
全仓（排除 data/dist/.git）无 debugger、无 TODO/FIXME/HACK/XXX。
全部 console.* 均为有意的 CLI/校验/运维日志（src/web/ 下浏览器代码为 0 个 console.log，仅 compare-models.js:281/327 用 console.warn 记录数据加载失败，属正确做法）。无需清理。
【重复与死代码】
CODEBASE-MAP.md:77-81 — tool-update-review-contract.js 与 tool-update-evidence.js 在 77-78 与 80-81 各重复列出一次（内容完全相同）；且 79 行 tool-update-review-planner.js 链接缺 src/catalog/ 前缀。磁盘上两文件各只有一份、职责不同（contract=审核五字段，evidence=日期解析），不是代码重复，是文档重复列。建议：删除重复两条，统一路径前缀。
src/catalog/tool-update-review-planner.js:238-239 — 重导出 explicitDates/dateForEvidence，全仓唯一消费者是测试 tests/catalog/tool-update-review.test.js:296；生产代码（catalog-date-repair.js:13、tool-update-collector.js:9）都直接引 evidence 原模块。建议：去掉这两个重导出别名。
src/catalog/tool-update-evidence.js:64-65 — dateForEvidence(evidence, maybeOptions, legacyOptions) 第三参 legacy 兼容 shim（Object.keys(legacyOptions).length ? legacyOptions : maybeOptions）是死分支：全仓只有 2 参调用（planner:184）。建议：删除 legacyOptions 参数。
过度暴露的导出（内部使用、无任何外部消费者）：
src/catalog/catalog-batch.js:514/520 — planBatchCost、estimateResolutionNeed
src/news/min/pipeline-min.js:501-502 — normalizeNow、resolveXWindow
src/comparison/rebuild-comparison.js:802/804/811 — buildModelRecord（外部仅 empty-model-filter.js:25 一处注释提及）、computeLmarenaEloBounds、loadInputs
src/web/js/search.js:957-989 — searchConceptHoverTimer、searchCitationOrigins、getSearchMatches、renderSearchResults、renderSearchFeedback（均未被 main.js/glossary.js/scenes.js import，只内部调用） 建议：或移出 export 语句，或加注释声明为公开接口。
scripts/catalog-date-audit.js 与 src/maintenance/catalog-date-audit.js — 职责无重叠：前者是 30 行薄包装（只转发 runCatalogDateAudit），符合 scripts/ 薄包装约定，无需处理。
src/catalog/tool-update-evidence.js:3 与 src/catalog/official-url-registry.js:196 — DATE_PATTERN = /^\d{4}-\d{2}-\d{2}$/ 常量重复定义两份。建议：并入共享模块。
抽查 catalog-batch/pipeline-min/rebuild-comparison 的 destructured require：经逐名 grep 核实均被使用（自写检测器因多行解构误报，已人工排除），无未使用 import。
【品牌残留】
.claude/CLAUDE.md:25 — 问题跟踪仍指向 wozore/InfoCatcher-Engineering（仓库已改名 wozore/KnowView）。建议核实 issues 归属；若 InfoCatcher-Engineering 仍是活跃 issues 仓则保留，否则更新。
.env.example:1 — 头注释「# ===== InfoCatcher 本地环境变量模板 =====」为旧品牌残留（仅注释、低优先）。
resources/survey/analysis.py:3 — 头注释「InfoCatcher 调查数据分析脚本」，历史调研产物，低优先。
开发日志.md — 历史日志，属允许例外。
public/、src/web/index.html、src/content/generate-rss.js:21 使用的 wozore.github.io/KnowView 均为当前正确 URL；.github/、README、package.json 无旧品牌。允许例外已核对，无漏网正式代码。
【前端小功能候选】
src/web/js/tools.js:427/433/442/448 — modal 关闭按钮 HTML 字符串 4 次完全重复（'<button class="modal-close" ...'）。建议抽成常量/helper。
内联 onclick="..." 依赖 window.* 全局（src/web/js/main.js:273-277 手工暴露 closeModal/toggleCompareRef/quickCompare）。新增内联 handler 若漏暴露会静默失效，且阻断 CSP。建议改 addEventListener。
src/web/js/tool-cards.js:49 — toggleCompareRef(card.detail_ref.id, card.detail_ref.id, this) 两参传相同值；toggleCompareRef(toolId, itemId) 的 toolId 形参名有误导（仅因 resolveCompareTarget 按 tool-level3: 前缀分支才不失效）。建议第一参传 card.tool_key。
src/web/js/compare-models.js:308 — 变体排序 (a.revision ? 1 : -1) - (b.revision ? 1 : -1) 只比较 revision 有无而非值，结果只分「无版本→有版本」两档，不按版本日期排序。建议核实意图，若需版本降序应改用 revision 值比较。
src/web/js/data.js:494-495 — '数据更新: ' 硬编码中文 UI 串，与 i18n 试点（t()/getLocalizedField）不一致。低优先。
已核对为健康无需改动：trending.js 加载失败保留旧数据+失败态（123-139）、compare-models.js 双 fetch 有 try/catch+失败态（279-331）、brand-icons 缓存+catch、modal 无障碍（focus 回焦/Escape/焦点陷阱/aria-label 齐全，main.js:760-805）、search.js 19 处 aria 属性+空态处理。data.js 各 JSON 每页只 fetch 一次（模块级状态），无重复拉取。
【其他】
测试缺口：pipeline-min 的 resolveXWindow/normalizeNow（X 采集窗口「北京时间今天 0 点 → now」默认窗口逻辑）无任何测试引用；对比之下 catalog-batch 成本门禁（dry-run 与无 confirmCost 零付费路径）已被 catalog-batch.test.js:145-177 覆盖。建议补 X 窗口纯函数测试。
.claude/worktrees/ — 17 个陈旧 worktree 副本、共 244MB（gitignored 本地杂项，非提交物）。MEMORY 记录 worktree 曾引发 CC Switch remote→本地回退误判，建议清理。
总评：无调试残留、无真实代码重复、无未使用 import、正式代码无漏网品牌。真正的清理价值集中在：导出面瘦身（第 4 条）、dateForEvidence 死分支与重导出别名（2、3 条）、CODEBASE-MAP 重复行（第 1 条）、陈旧 worktree 磁盘清理（第 20 条）、以及 X 窗口测试补缺（第 19 条）。

三路探测全部回来了。仓库整体健康度不错——.env 无泄漏、无调试残留、无真实代码重复、正式代码无漏网品牌。问题集中且大多好处理，我按「可直接做 / 需你拍板」分档：

A. 机械清理（低风险，我可以直接做）
CODEBASE-MAP.md 修正——文档重复条目 2 对（tool-update-review-contract.js/tool-update-evidence.js 各列两次）、catalog-interface.js 段归属错误、漏了采集实际入口 scripts/build-news.js（map 完全没收录）、archive-min.bat、4 个 src 模块（含 beijing-time.js）、约 25 个测试文件
删空目录/损坏项：.smoke-cmp2/3/4 空目录、.obsidian 损坏符号链接（指向目标已不存在）、data/catalog 事务残留目录
清 .gitignore 死规则（指向已不存在的旧目录结构）
B. 代码优化（小批量、各独立 commit，随时可做）
导出面瘦身：planBatchCost/estimateResolutionNeed/normalizeNow/resolveXWindow 等 5 组内部导出无任何外部消费者
死分支：dateForEvidence 的 legacyOptions 兼容 shim 无调用方；planner 的 explicitDates/dateForEvidence 重导出只有测试在用
DATE_PATTERN 常量在 evidence / official-url-registry 两份重复
前端小优化：tools.js modal-close HTML 重复 4 次、内联 onclick 依赖全局、tool-cards.js toggleCompareRef 第一参传错、compare-models.js 变体排序不按版本值
测试补缺：resolveXWindow/normalizeNow（X 采集窗口纯函数）零测试覆盖

已落盘，下次对话我会带着这份清单继续。当前暂不动。

待办清单（已保存，下次对话统一修复）
A. 机械清理（低风险，可直接做）
CODEBASE-MAP.md：删 2 对重复条目（tool-update-review-contract.js/tool-update-evidence.js）、修 catalog-interface.js 段归属、补录 build-news.js（采集实际入口，map 零命中）、archive-min.bat、4 个 src 模块、约 25 个测试文件
删空目录：.smoke-cmp2/3/4、data/catalog 事务残留、.obsidian 损坏符号链接
清 .gitignore 死规则；.claudian/ 改整目录忽略
废弃分支：6 个本地 worktree 分支 + 10 个远端 news/review/batch-*
B. 代码优化（小批量、各独立 commit）
导出面瘦身（5 组内部导出无外部消费者）、dateForEvidence 死分支、planner 重导出别名、DATE_PATTERN 常量重复两份
前端：modal-close HTML 重复 4 次、内联 onclick→addEventListener、toggleCompareRef 传参、变体排序、硬编码中文串
测试补缺：resolveXWindow/normalizeNow 零覆盖
C. 需你拍板（推荐已定）
根目录三个中文文件 → 推荐归档 resources/archive/（保留历史可找回）
min-candidates.json（5.7MB）→ 推荐暂不动，测试期后单独立项
两个孤立脚本 → 推荐归档 docs/archive/
INFOCATCHER_* 环境变量 → 推荐宣发后再定，要改需双读兼容
D. 品牌残余（需确认）
.claude/CLAUDE.md 仍指向 wozore/InfoCatcher-Engineering issue 仓（工程 issue 仓，需确认保留还是更新）
.env.example、resources/survey/analysis.py 头注释旧品牌（仅注释，低优先）
E. 磁盘清理（本地，不入库）
.claude/worktrees/ 17 个副本 244MB、E:/Work/knowview-ci-check 旧 worktree
结论：仓库整体健康（.env 无泄漏、正式代码无旧品牌、无死代码/调试残留），这些是优化而非紧急修复。CI 已绿，可以随时开始熟人轻度测试。

测试这块要我准备什么？比如：给测试对象的一页测试要点清单（重点验收路径 + 已知边界 + 反馈怎么提交），或者先搭一个简单的内测反馈收集结构。