---
name: files-create-check
description: 在 Claude Code 准备于当前项目中创建文件、进行多处/批量修改或批量删除文件时，先向用户说明具体操作计划并取得确认，再执行并核验结果。只要任务会直接或间接新建一个或多个文件，必须使用本技能；当任务要修改同一文件中的多个独立内容区域、修改两个及以上现有文件，或删除两个及以上现有文件时，也必须使用本技能。创建计划须列出文件路径和功能，修改计划须列出文件路径及具体修改部分，删除计划须列出目标文件及删除原因。即使操作由 Write、Edit、脚本、构建命令、生成器、格式化器或其他工具间接完成，也要触发。不要用于仅修改单个现有文件中的一个内容点、仅删除单个文件、仅创建文件夹、只读搜索、移动或重命名且不会生成新文件的任务。
---

# 项目文件创建、批量修改与批量删除检查

## 目标

在当前项目根目录及其子目录中创建文件、进行多处修改或批量删除前，让用户清楚知道即将发生什么；取得确认后再执行，并在完成时准确报告实际结果。

本技能覆盖三类操作：

1. 创建一个或多个新文件；
2. 修改同一文件中的多个独立内容区域，或修改两个及以上现有文件；
3. 删除两个及以上现有文件。

项目外文件不在本技能的核验范围内。

## 触发判定

出现以下任一情况就使用本技能：

- 直接创建一个或多个文件；
- 通过脚本、命令、构建、测试、导出、模板或代码生成器间接产生文件；
- 同一任务既创建文件又修改或删除现有文件；
- 修改同一现有文件中的两个或更多独立内容点、章节、配置项或代码区域；
- 修改两个或更多现有文件；
- 删除两个或更多现有文件，包括通过脚本、通配符、清理命令或生成工具间接删除。

以下情况不触发：

- 只修改一个现有文件中的一个内容点；
- 只删除一个现有文件；
- 只创建文件夹而不创建文件；
- 只读取、搜索或分析；
- 只移动或重命名现有文件，且不会产生新文件。

如果无法确定一次修改是否包含多个独立内容点，或一次删除是否会影响多个文件，按会触发处理，因为提前说明比未经确认扩大操作范围更安全。

## 工作流程

### 1. 调查现状，但不要先行改动

在征求确认前，使用只读操作确定：

- 目标文件是否已经存在；
- 需要修改的章节、函数、配置项或其他内容区域；
- 生成器可能产生的文件路径或输出范围；
- 删除目标的实际路径、数量和用途，尤其是通配符、目录清理或脚本计算出的目标。

删除是难以撤销的操作。提出计划前必须查看目标，确认它们与用户描述一致；如果实际目标、数量或用途与描述不符，先向用户说明差异，不要继续。

不要在调查阶段创建、修改或删除文件。

### 2. 向用户说明计划并请求确认

执行任何创建、多处修改或批量删除前，先向用户展示计划。

创建文件时使用：

```markdown
## 计划创建

- [项目相对路径]：该文件将提供的功能或用途。

是否按以上计划创建？
```

修改文件时使用：

```markdown
## 计划修改

- [项目相对路径]
  - 修改部分：准备修改的章节、函数、配置项或内容区域。
  - 修改目的：这项修改要实现或修正什么。

是否按以上计划修改？
```

批量删除文件时使用：

```markdown
## 计划删除

- [项目相对路径]：文件当前用途；删除原因。

以上文件删除后可能无法恢复。是否确认删除？
```

逐项列出已经确定的删除目标。不要仅写文件夹或通配符来代替实际文件清单；如果目标只能在命令运行时确定，先用只读方式展开并展示匹配结果。目标很多时可以按用途分组，但仍要让用户看清具体文件路径和总数。

混合任务按实际情况同时列出“计划创建”“计划修改”和“计划删除”。对于输出路径无法完全预知的创建工具，不要把输出文件夹伪装成待创建文件；改用“预计输出范围”说明可确认的输出目录、文件名模式或文件类型、预计数量，以及每类输出文件的功能。例如：

```markdown
## 预计输出范围

- 输出位置：`generated/`
- 文件范围：由生成脚本定义的 3 个样例文件（具体名称执行后核验）。
- 文件功能：提供后续测试或演示所需的样例数据。

是否运行生成操作？
```

用户明确同意后才执行。如果用户调整范围，更新计划并按新范围执行；如果用户拒绝，则停止。用户在当前对话中已经针对同一份具体计划明确授权时，不要重复询问。

### 3. 跟踪实际操作

执行期间维护创建、修改和删除记录：

- 对计划创建的明确路径，记录执行前是否存在；
- 对生成器等动态输出，比较相关输出范围执行前后的文件清单；
- 区分新文件与原有文件的覆盖或编辑；
- 删除后核验每个目标是否确实不存在，同时检查未授权文件是否保持不变；
- 批处理只完成一部分时，逐项记录成功与失败；
- 不把文件夹当作文件操作结果。

如果执行过程中发现新增目标或操作范围扩大，暂停并重新说明增量计划，取得确认后再继续。

### 4. 核验并报告结果

完成后按实际状态报告，不要把未核验的操作声称为成功。

```markdown
## 成功创建

- [项目相对路径]：该文件的功能。

## 创建失败

- [项目相对路径]：计划用途；失败原因。

## 成功修改

- [项目相对路径]：实际修改的部分及作用。

## 修改失败

- [项目相对路径]：计划修改的部分；失败原因。

## 成功删除

- [项目相对路径]：原有用途；删除原因。

## 删除失败

- [项目相对路径]：计划删除原因；失败原因。
```

输出规则：

- 使用相对于项目根目录的路径；
- 每项都简要说明功能、修改部分、删除原因或失败原因；
- 没有对应项目时写“无”；
- 不列文件夹；
- 不把执行前已存在的文件误报为新文件；
- 不把仍然存在的文件报告为删除成功；
- 除非用户另有要求，不在结果清单之外重复长篇过程说明。
