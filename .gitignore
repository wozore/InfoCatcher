# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# 测试 / 类型检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.hypothesis/

# Jupyter
.ipynb_checkpoints/

# ===== 操作系统生成文件 =====
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# ===== 编辑器 / IDE =====
.idea/
.vscode/
*.swp
*.swo
*~

# ===== 人工维护内容（data/manual） =====
# 热点管线 v2 需人工修改的内容（字幕清单 / 关键词提纯候选 / 待补工具卡等）
# 集中放在 data/manual/，固定格式生成。约定：入库（维护者可见/可提交），
# 但 build-dist.js 不复制到 dist/（不发布到站点）。此目录无需忽略规则——
# 空目录 git 不追踪，有文件即入库；留此注释说明意图，防误加忽略。
# ===== 目录生成器临时文件 =====
data/manual/catalog-drafts/
config/catalog-generator.local.json
data/catalog/.catalog.lock
data/catalog/.transactions/
data/catalog/.staging/
data/catalog/.backup/
# 工程文档（架构/需求/运维/归档/原型等）整体不入库；
# 只允许用户说明类 md 入库，约定放在 docs/manual/ 下
docs/*
!docs/manual/
docs/manual/**
!docs/manual/**/*.md

# ===== 第三方 Skills 插件缓存 =====
# mattpocock-skills 等第三方 skill 含配置文件，不入库
.claude/skills/

# ===== Claude Code / AI 工具会话数据 =====
# 会话元数据频繁变化，不入库
.claudian/sessions/

# ===== Claude Code 临时 worktree =====
# Claude Code 创建的嵌套 worktree 是独立 Git 工作区，不应作为主仓库 gitlink 提交
.claude/worktrees/

# ===== Obsidian =====
# .obsidian 为符号链接，指向外部笔记库（MyNote/AI归档知识库），非工程交付物；
# 整个目录忽略（含符号链接文件形态与目录形态），取消历史误跟踪的配置（app.json 等）
.obsidian
.obsidian/

# ===== 脚本生成的临时输出 =====
# 分析脚本运行产生的中间/输出文件（若生成到这些位置）
*.tmp
*.bak
/output/
/outputs/

# ===== 敏感信息（API Key / 环境变量） =====
.env
.env.*
!.env.example
*.secret
*_secret.*
credentials.json
service-account.json
*.log

# ===== 调查原始数据（含受访者个人信息，不入库） =====
# 已脱敏的编码 CSV、问卷 PDF 和分析脚本在 resources/survey/ 下受版本控制
# ===== MVP 独立仓库（避免嵌套 .git 冲突） =====
# mvp/ 源码仍由工程仓库追踪（属于 06.编码与单元测试 的交付物），
# 仅排除内层 .git 目录，防止两个 git 仓库交叉管理
06.编码与单元测试/mvp/.git/

logs/

# MVP 热点构建临时锁（运行时生成，不入库）
06.编码与单元测试/mvp/data/news/runtime/.news-build.lock
